#lang info

(define collection "NHS Number Validator")

(define deps '("base"))

(define build-deps '("rackunit-lib" "scribble-lib" "racket-doc" "typed-racket-lib"))

(define version "0.2")

(define primary-file "nhs-number.rkt")

